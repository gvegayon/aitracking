% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/traffic.R
\name{gh_traffic}
\alias{gh_traffic}
\title{Retrieve repository traffic (clones or views)}
\usage{
gh_traffic(
  repo,
  metric = c("clones", "views"),
  per = c("day", "week"),
  token = gh_token()
)
}
\arguments{
\item{repo}{Character vector of repositories in \code{"owner/repo"} form.}

\item{metric}{Either \code{"clones"} (default) or \code{"views"}.}

\item{per}{Either \code{"day"} (default) or \code{"week"}.}

\item{token}{GitHub token (see \code{\link[=gh_token]{gh_token()}}). Note that the traffic API
requires \strong{push access} to the repository.}
}
\value{
A \code{data.table} with columns \code{repo}, \code{date} (\code{POSIXct}, UTC),
\code{count}, and \code{uniques}.
}
\description{
Downloads the clone or view history of one or more repositories from
GitHub's traffic API.
}
\details{
GitHub only stores traffic data for the last 14 days, so building a longer
clone history requires periodic snapshots (e.g., a scheduled GitHub Action
that appends the output of this function to a file).

For download counts of release assets--which GitHub does keep
indefinitely--see \code{\link[=gh_downloads]{gh_downloads()}}.
}
\examples{
\dontrun{
gh_traffic("UofUEpiBio/epiworld") # clones, last 14 days
gh_traffic("UofUEpiBio/epiworld", metric = "views", per = "week")
}
}
\seealso{
Other retrieval:
\code{\link[=gh_commit_lines]{gh_commit_lines()}},
\code{\link[=gh_commits]{gh_commits()}},
\code{\link[=gh_downloads]{gh_downloads()}},
\code{\link[=gh_interactions]{gh_interactions()}},
\code{\link[=gh_languages]{gh_languages()}}
}
\concept{retrieval}
