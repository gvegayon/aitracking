% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/commits.R
\name{gh_commits}
\alias{gh_commits}
\title{Retrieve the commit history of one or more repositories}
\usage{
gh_commits(
  repo,
  since = NULL,
  until = NULL,
  path = NULL,
  token = gh_token(),
  max_pages = Inf
)
}
\arguments{
\item{repo}{Character vector of repositories in \code{"owner/repo"} form, e.g.,
\code{"UofUEpiBio/epiworld"}. When more than one repository is passed, the
results are stacked (the \code{repo} column identifies each one).}

\item{since, until}{Optional lower/upper bounds for the commit time. Can be
\code{Date}, \code{POSIXct}, or character (\code{"YYYY-MM-DD"} or full ISO 8601).}

\item{path}{Optional character scalar. Only commits touching this file path
are returned.}

\item{token}{GitHub token (see \code{\link[=gh_token]{gh_token()}}).}

\item{max_pages}{Number of pages (of 100 commits each) to retrieve at most
per repository. Defaults to \code{Inf} (the full history).}
}
\value{
A \code{data.table} with one row per commit, sorted from oldest to
newest, and columns:
\itemize{
\item \code{repo}: the repository (\code{"owner/repo"}).
\item \code{sha}: commit hash.
\item \code{author}: GitHub login of the author (\code{NA} if not linked to an account).
\item \code{author_name}, \code{author_email}: name/email recorded in the commit.
\item \code{committer}: name of the committer (e.g., \code{"GitHub"} for squash merges
done through the web interface).
\item \code{date}: author timestamp as \code{POSIXct} (UTC).
\item \code{message}: full commit message.
}
}
\description{
Downloads the commit history of one or more GitHub repositories as a
\link[data.table:data.table]{data.table::data.table}. By default the entire history is retrieved; use
\code{since}/\code{until} (or \code{max_pages}) to subset it.
}
\details{
Line counts are not part of GitHub's commit-list endpoint; pipe the result
into \code{\link[=gh_commit_lines]{gh_commit_lines()}} to add them.
}
\examples{
\dontrun{
commits <- gh_commits("UofUEpiBio/epiworld", since = "2024-01-01")

# Piping into the classifier and the plotting function
gh_commits("UofUEpiBio/epiworld") |>
  ai_classify() |>
  plot_timeline()
}
}
\seealso{
Other retrieval:
\code{\link[=gh_commit_lines]{gh_commit_lines()}},
\code{\link[=gh_downloads]{gh_downloads()}},
\code{\link[=gh_interactions]{gh_interactions()}},
\code{\link[=gh_languages]{gh_languages()}},
\code{\link[=gh_traffic]{gh_traffic()}}
}
\concept{retrieval}
