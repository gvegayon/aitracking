% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{gh_token}
\alias{gh_token}
\title{Find a GitHub API token}
\usage{
gh_token(token = NULL)
}
\arguments{
\item{token}{Optional character scalar. If non-empty, it is returned as-is.}
}
\value{
A character scalar with the token, or the empty string \code{""} when no
token was found (requests then go out unauthenticated).
}
\description{
Looks for a GitHub personal access token (PAT) in the following order:
the \code{token} argument itself, the \code{GITHUB_PAT} environment variable, the
\code{GITHUB_TOKEN} environment variable, and--if the \pkg{gitcreds} package is
installed--the git credential store (the same one used by the \pkg{gh}
package and the \code{gh} command line client).
}
\details{
All API-calling functions in \pkg{aitracking} take a \code{token} argument that
defaults to \code{gh_token()}, so in most setups authentication "just works":
either because \code{GITHUB_PAT}/\code{GITHUB_TOKEN} is set, or because a token is
available from the git credential store.

Unauthenticated requests are possible but heavily rate-limited by GitHub
(60 requests/hour versus 5,000/hour with a token).
}
\examples{
\dontrun{
gh_token() # What token would be used?
}
}
\seealso{
Other api:
\code{\link[=gh_api]{gh_api()}}
}
\concept{api}
