% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/languages.R
\name{loc_evolution}
\alias{loc_evolution}
\title{Evolution of project size (lines of code) over time}
\usage{
loc_evolution(x)
}
\arguments{
\item{x}{A \code{data.frame}/\code{data.table} with columns \code{date}, \code{additions}, and
\code{deletions}--i.e., the output of \code{\link[=gh_commit_lines]{gh_commit_lines()}}. If a \code{file} column is
present (from \code{gh_commit_lines(files = TRUE)}), the result is computed by
language.}
}
\value{
A \code{data.table} with columns \code{repo}, \code{date}, \code{delta} (net lines
changed at that time), and \code{loc} (cumulative net lines), plus \code{language}
when \code{x} has file-level detail. Sorted by time within \code{repo} (and
\code{language}).
}
\description{
Computes the cumulative net lines of code (additions minus deletions) of a
project over time from its commit history. With file-level input (from
\code{gh_commit_lines(x, files = TRUE)}), the evolution is broken down by
language, mapped from file extensions.
}
\details{
The measure is approximate: it counts net \emph{lines changed} as reported by
GitHub, which includes documentation, data, and other non-code files
(unless you filter them out beforehand). Commits with missing statistics
contribute zero.
}
\examples{
\dontrun{
gh_commits("UofUEpiBio/epiworld") |>
  gh_commit_lines(files = TRUE) |>
  loc_evolution()
}
}
\seealso{
Other analysis:
\code{\link[=ai_classify]{ai_classify()}},
\code{\link[=ai_patterns]{ai_patterns()}},
\code{\link[=plot_timeline]{plot_timeline()}}
}
\concept{analysis}
