% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copilot.R
\name{gh_copilot_seats}
\alias{gh_copilot_seats}
\title{Retrieve Copilot seat assignments for an organization}
\usage{
gh_copilot_seats(org, token = gh_token(), max_pages = Inf)
}
\arguments{
\item{org}{Character vector of organization logins.}

\item{token}{GitHub token (see \code{\link[=gh_token]{gh_token()}}).}

\item{max_pages}{Number of pages (of 100 seats each) to retrieve at most.}
}
\value{
A \code{data.table} with columns \code{org}, \code{user}, \code{created_at},
\code{last_activity_at}, \code{last_activity_editor}, and \code{plan_type}. An
organization with no Copilot seats contributes no rows.
}
\description{
Downloads the list of Copilot seats assigned in an organization, including
when each seat was created and when the user was last active. This is the
most widely accessible Copilot endpoint: unlike \code{\link[=gh_copilot_metrics]{gh_copilot_metrics()}} it
needs only \code{read:org} (plus organization ownership) and no usage-metrics
policy.
}
\details{
\code{last_activity_at} and \code{last_activity_editor} are the closest thing the
API offers to "how much has this person used Copilot": they tell you
whether and where a seat is being exercised, but not how much.
}
\examples{
\dontrun{
gh_copilot_seats("my-org")
}
}
\seealso{
Other copilot:
\code{\link[=gh_copilot_metrics]{gh_copilot_metrics()}}
}
\concept{copilot}
