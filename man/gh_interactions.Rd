% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interactions.R
\name{gh_interactions}
\alias{gh_interactions}
\title{Retrieve user interactions within a repository}
\usage{
gh_interactions(repo, since = NULL, token = gh_token(), max_pages = Inf)
}
\arguments{
\item{repo}{Character vector of repositories in \code{"owner/repo"} form.}

\item{since}{Optional lower time bound (\code{Date}, \code{POSIXct}, or character).
Note that, following the GitHub API, for issues/PRs this filters by \emph{last
update} time, and for comments by \emph{creation} time.}

\item{token}{GitHub token (see \code{\link[=gh_token]{gh_token()}}).}

\item{max_pages}{Number of pages (of 100 records each) to retrieve at most
per repository and record type.}
}
\value{
A \code{data.table} with one row per interaction, sorted by time, and
columns:
\itemize{
\item \code{repo}: the repository.
\item \code{type}: one of \code{"issue"}, \code{"pull_request"}, \code{"issue_comment"} (comments
on issues \emph{and} PRs), or \code{"review_comment"} (inline code-review comments
on PRs).
\item \code{number}: the issue/PR number the interaction belongs to.
\item \code{user}: GitHub login of the user (bots show as, e.g.,
\code{"copilot[bot]"}).
\item \code{created_at}: creation time (\code{POSIXct}, UTC).
\item \code{title}: issue/PR title (\code{NA} for comments).
\item \code{body}: full text of the issue/PR description or comment.
}
}
\description{
Downloads issues, pull requests, issue/PR comments, and pull request review
comments of one or more repositories. This is the conversation layer of a
project--including the requests humans make to AI agents in issues and
PRs--which survives even when the commits themselves are squash-merged.
}
\details{
Combine with \code{\link[=ai_classify]{ai_classify()}} to flag which interactions were authored by AI
agents (\code{ai}) and which ones mention or address an AI agent
(\code{ai_mention})--the latter is a simple proxy for how much humans are
prompting AI within the repository.
}
\examples{
\dontrun{
gh_interactions("UofUEpiBio/epiworld") |>
  ai_classify()
}
}
\seealso{
Other retrieval:
\code{\link[=gh_commit_lines]{gh_commit_lines()}},
\code{\link[=gh_commits]{gh_commits()}},
\code{\link[=gh_downloads]{gh_downloads()}},
\code{\link[=gh_languages]{gh_languages()}},
\code{\link[=gh_traffic]{gh_traffic()}}
}
\concept{retrieval}
