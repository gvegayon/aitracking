% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copilot.R
\name{gh_copilot_metrics}
\alias{gh_copilot_metrics}
\title{Retrieve Copilot usage metrics for an organization or enterprise}
\usage{
gh_copilot_metrics(
  owner,
  report = c("organization-28-day", "organization-1-day", "users-28-day", "users-1-day",
    "repos-1-day", "user-teams-1-day", "enterprise-28-day", "enterprise-1-day"),
  day = NULL,
  level = c("orgs", "enterprises"),
  token = gh_token()
)
}
\arguments{
\item{owner}{Character scalar. Organization login or enterprise slug.}

\item{report}{One of \code{"organization-28-day"} (default),
\code{"organization-1-day"}, \code{"users-28-day"}, \code{"users-1-day"},
\code{"repos-1-day"}, or \code{"user-teams-1-day"}. Enterprise reports use
\code{"enterprise-28-day"}/\code{"enterprise-1-day"} in place of the organization
ones.}

\item{day}{Date (or \code{"YYYY-MM-DD"}) for the \verb{*-1-day} reports. Metrics are
processed once per day for the previous day, so the most recent day
available is yesterday.}

\item{level}{Either \code{"orgs"} (default) or \code{"enterprises"}.}

\item{token}{GitHub token (see \code{\link[=gh_token]{gh_token()}}).}
}
\value{
A list with the parsed API response. For 28-day reports this is
the report metadata (\code{report_start_day}, \code{report_end_day}) plus
\code{download_links}; for 1-day reports, \code{report_day} plus \code{download_links}.
The metric values themselves live in the files behind \code{download_links},
which are pre-signed URLs you fetch separately (they are not part of the
JSON response).
}
\description{
Wrapper around GitHub's Copilot usage metrics API, which reports how much
Copilot is actually being \emph{used}---code suggestions and acceptances, chat
activity, active users---as opposed to what \pkg{aitracking}'s other
functions infer from repository history.
}
\section{Access requirements}{

This endpoint is gated well beyond a normal token, which is why it is not
usable for arbitrary public repositories:
\itemize{
\item The \strong{"Copilot usage metrics" policy must be enabled} for the
organization/enterprise. Without it every request fails with
\verb{HTTP 403} regardless of your token.
\item You must be an \strong{organization owner} (or enterprise owner/billing
manager), with \code{read:org} for organization reports and
\code{manage_billing:copilot} or \code{read:enterprise} for enterprise ones.
}

There is \strong{no public, per-repository endpoint} for suggestion or token
counts: metrics are aggregated at the organization/enterprise level (the
\code{repos-1-day} report breaks pull request activity down by repository, but
only within an org you administer). Nothing exposes "tokens spent" or
"hours used"; the closest available quantities are suggestion/acceptance
counts, lines suggested/accepted, and active/engaged user counts. For
third-party repositories, the history-based measures in this package
(see \code{\link[=ai_classify]{ai_classify()}}) are the only option.
}

\examples{
\dontrun{
# Requires org ownership *and* the usage-metrics policy enabled
m <- gh_copilot_metrics("my-org")
m$download_links

# Per-repository pull request metrics for a given day
gh_copilot_metrics("my-org", report = "repos-1-day", day = Sys.Date() - 1)
}
}
\seealso{
\code{\link[=gh_copilot_seats]{gh_copilot_seats()}} for seat assignment, which needs only
\code{read:org} and is therefore usable much more often.

Other copilot:
\code{\link[=gh_copilot_seats]{gh_copilot_seats()}}
}
\concept{copilot}
