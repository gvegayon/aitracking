% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assignments.R
\name{gh_pulls}
\alias{gh_pulls}
\title{Retrieve pull requests, including their head branch}
\usage{
gh_pulls(
  repo,
  state = c("all", "open", "closed"),
  token = gh_token(),
  max_pages = Inf
)
}
\arguments{
\item{repo}{Character vector of repositories in \code{"owner/repo"} form.}

\item{state}{One of \code{"all"} (default), \code{"open"}, or \code{"closed"}.}

\item{token}{GitHub token (see \code{\link[=gh_token]{gh_token()}}).}

\item{max_pages}{Number of pages (of 100 PRs each) to retrieve at most per
repository.}
}
\value{
A \code{data.table} with one row per pull request, sorted by time, and
columns \code{repo}, \code{number}, \code{title}, \code{user} (who opened it), \code{branch} (the
head ref), \code{base} (the target branch), \code{state}, \code{draft}, \code{created_at},
\code{merged_at}, and \code{closed_at}.
}
\description{
Downloads the pull requests of one or more repositories. Unlike
\code{\link[=gh_interactions]{gh_interactions()}}, which sees PRs through the issues endpoint, this
function returns the \strong{head branch name}, which is one of the more
reliable traces of AI involvement: coding agents push to prefixed branches
such as \code{copilot/fix-thing} or \code{codex/add-tests}, and that prefix survives
even when a human opens the pull request and squash-merges it.
}
\details{
\code{\link[=ai_classify]{ai_classify()}} scans a \code{branch} column by default, so piping the result
straight into it picks up the branch-prefix signal on top of the usual
identity and commit-trailer rules.
}
\examples{
\dontrun{
# Which PRs came off an agent branch, and who opened them?
gh_pulls("UofUEpiBio/epiworld") |>
  ai_classify() |>
  subset(ai, select = c(number, user, branch, ai_agent))
}
}
\seealso{
Other retrieval:
\code{\link[=gh_assignments]{gh_assignments()}},
\code{\link[=gh_commit_lines]{gh_commit_lines()}},
\code{\link[=gh_commits]{gh_commits()}},
\code{\link[=gh_downloads]{gh_downloads()}},
\code{\link[=gh_interactions]{gh_interactions()}},
\code{\link[=gh_languages]{gh_languages()}},
\code{\link[=gh_traffic]{gh_traffic()}}
}
\concept{retrieval}
