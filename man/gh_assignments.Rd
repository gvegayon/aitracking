% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assignments.R
\name{gh_assignments}
\alias{gh_assignments}
\title{Retrieve issue and pull request assignments}
\usage{
gh_assignments(
  repo,
  state = c("all", "open", "closed"),
  since = NULL,
  ai_only = FALSE,
  patterns = ai_patterns(),
  token = gh_token(),
  max_pages = Inf
)
}
\arguments{
\item{repo}{Character vector of repositories in \code{"owner/repo"} form.}

\item{state}{One of \code{"all"} (default), \code{"open"}, or \code{"closed"}.}

\item{since}{Optional lower bound on the \emph{last update} time (\code{Date},
\code{POSIXct}, or character), following the GitHub API.}

\item{ai_only}{Logical scalar. When \code{TRUE}, only assignments to AI agents
are returned.}

\item{patterns}{Named character vector of agent patterns used to decide
what counts as an AI assignee. See \code{\link[=ai_patterns]{ai_patterns()}}.}

\item{token}{GitHub token (see \code{\link[=gh_token]{gh_token()}}).}

\item{max_pages}{Number of pages (of 100 records each) to retrieve at most
per repository.}
}
\value{
A \code{data.table} with \strong{one row per issue/assignee pair}, sorted by
time, and columns:
\itemize{
\item \code{repo}, \code{type} (\code{"issue"} or \code{"pull_request"}), \code{number}, \code{title}.
\item \code{user}: login of the account that \emph{opened} the issue/PR.
\item \code{assignee}: login of the assigned account, \code{NA} when nobody is assigned.
\item \code{assignee_type}: \code{"User"} or \code{"Bot"}, as reported by GitHub.
\item \code{assigned_ai}: logical, \code{TRUE} when the assignee matches an AI agent.
\item \code{assigned_agent}: which agent (\code{NA} otherwise).
\item \code{state}, \code{created_at}, \code{closed_at}.
}

Issues with several assignees contribute one row each; unassigned issues
contribute a single row with \code{assignee = NA}, so that shares (e.g., the
fraction of issues delegated to an agent) can be computed from the same
table.
}
\description{
Downloads the issues and pull requests of one or more repositories together
with the accounts they are assigned to, and flags the assignments handed to
an AI coding agent. Assigning an issue to an agent (e.g., selecting
"Copilot" in the Assignees box) is how GitHub's coding agent is asked to do
a task, so these rows are a direct measure of \emph{delegation} to AI, as
opposed to the AI involvement in the resulting code that \code{\link[=gh_commits]{gh_commits()}}
plus \code{\link[=ai_classify]{ai_classify()}} measure.
}
\details{
Note that GitHub reports the Copilot coding agent as the login \code{Copilot}
with \code{assignee_type == "Bot"}, \emph{without} the usual \verb{[bot]} suffix (its
underlying account is \code{copilot-swe-agent[bot]}). Matching on
\code{assignee_type} alone is not enough either: some agents (e.g.,
\code{cursoragent}, \code{openhands-agent}) are plain \code{"User"} accounts. This is why
the default detection is pattern-based; see \code{\link[=ai_patterns]{ai_patterns()}}.
}
\examples{
\dontrun{
# Everything delegated to an AI agent
gh_assignments("UofUEpiBio/epiworld", ai_only = TRUE)

# Share of issues/PRs delegated to an agent
a <- gh_assignments("UofUEpiBio/epiworld")
mean(a$assigned_ai)
}
}
\seealso{
Other retrieval:
\code{\link[=gh_commit_lines]{gh_commit_lines()}},
\code{\link[=gh_commits]{gh_commits()}},
\code{\link[=gh_downloads]{gh_downloads()}},
\code{\link[=gh_interactions]{gh_interactions()}},
\code{\link[=gh_languages]{gh_languages()}},
\code{\link[=gh_pulls]{gh_pulls()}},
\code{\link[=gh_traffic]{gh_traffic()}}
}
\concept{retrieval}
