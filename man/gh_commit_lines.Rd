% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/commits.R
\name{gh_commit_lines}
\alias{gh_commit_lines}
\title{Retrieve lines added/deleted per commit}
\usage{
gh_commit_lines(x, files = FALSE, token = gh_token(), verbose = interactive())
}
\arguments{
\item{x}{Either a \code{data.frame}/\code{data.table} with columns \code{repo} and \code{sha}
(as returned by \code{\link[=gh_commits]{gh_commits()}}), or a character vector of repositories, in
which case \code{\link[=gh_commits]{gh_commits()}} is called first.}

\item{files}{Logical scalar. When \code{TRUE}, returns one row per file changed
per commit instead of one row per commit.}

\item{token}{GitHub token (see \code{\link[=gh_token]{gh_token()}}).}

\item{verbose}{Logical scalar. Print a progress bar? (One API call is made
per commit, so this can take a while for long histories.)}
}
\value{
When \code{files = FALSE} (the default), a copy of \code{x} as a \code{data.table}
with two new integer columns, \code{additions} and \code{deletions}. When
\code{files = TRUE}, a \code{data.table} with columns \code{repo}, \code{sha}, \code{date} (if
available in \code{x}), \code{file}, \code{status}, \code{additions}, and \code{deletions}.
}
\description{
Adds per-commit line counts (\code{additions}, \code{deletions}) to a commit history,
or--with \code{files = TRUE}--returns the file-level detail of each commit.
Designed to be piped from \code{\link[=gh_commits]{gh_commits()}}.
}
\details{
GitHub's commit-list endpoint does not include line counts, so this
function requests each commit individually (\verb{/repos/\{repo\}/commits/\{sha\}}),
i.e., one API call per commit. With an authenticated token the rate limit
is 5,000 calls/hour. For very large projects, consider subsetting the
commit history first.
}
\examples{
\dontrun{
commits <- gh_commits("UofUEpiBio/epiworld") |>
  gh_commit_lines()

# File-level detail, useful for loc_evolution(<...>) by language
files <- gh_commits("UofUEpiBio/epiworld") |>
  gh_commit_lines(files = TRUE)
}
}
\seealso{
Other retrieval:
\code{\link[=gh_commits]{gh_commits()}},
\code{\link[=gh_downloads]{gh_downloads()}},
\code{\link[=gh_interactions]{gh_interactions()}},
\code{\link[=gh_languages]{gh_languages()}},
\code{\link[=gh_traffic]{gh_traffic()}}
}
\concept{retrieval}
